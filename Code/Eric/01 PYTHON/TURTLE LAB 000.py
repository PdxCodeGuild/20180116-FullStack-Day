from turtle import *

move_distance = 100
turn_angle = 144

forward (move_distance)
right(turn_angle)
forward (move_distance)
right(turn_angle)
forward (move_distance)
right(turn_angle)
forward (move_distance)
right(turn_angle)
forward (move_distance)
right(turn_angle)

done()