


let convert = prompt('What unit would you like to convert from?')
let convertTo = prompt('To what unit?')
let distance = prompt('How many?')
var toMeters = [feet:0.3048, mile:1609.04, kilometer:1000]

function unitConverter(){
    if (convert === meter){
        document.write(convert * toMeters.convertTo);
    }else if(convert != meter){
        let newDistance = (distance * to_meters.convertTo);

    }
}